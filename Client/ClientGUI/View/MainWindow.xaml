<controls:MetroWindow x:Class="ClientGUI.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    Title="Client GUI" 
    Height="400" 
    Width="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <Label HorizontalAlignment="Center">Select game</Label>
        
        <ComboBox Grid.Column="1" Grid.Row="0" Height="40"
                  ItemsSource="{Binding GamesList}" 
                  SelectedItem="{Binding SelectedGame}"  />
        
        <UserControl Content="{Binding SelectedGameControl}"  
                     VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" 
                     HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" 
                     Margin="15"
                     Grid.Row="1" Grid.Column="1"/>
    </Grid>
</controls:MetroWindow>

